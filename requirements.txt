# Core — required to run `streamlit run app.py` with defaults
# (EMBEDDING_BACKEND=tfidf, LLM_BACKEND=extractive => no API keys needed)
streamlit
pypdf
google-genai
numpy
sentence-transformers
requests>=2.31.0
chromadb>=0.5
scikit-learn>=1.4

# Optional — only needed if you set LLM_BACKEND=anthropic
anthropic>=0.34

# Optional — only needed if you set LLM_BACKEND=openai or EMBEDDING_BACKEND=openai
# openai>=1.40

# Optional — only needed if you set EMBEDDING_BACKEND=local (large download)
# sentence-transformers>=3.0

# Optional — only needed if you also deploy api.py as a separate REST service
# fastapi>=0.112
# uvicorn>=0.30
# python-multipart>=0.0.9

